<div class="jumbotron">
  <h1>New System Token</h1>
  <p>
    We just need to get the name of the system that you want to give access to your gems.

    <% @new_device.errors.full_messages.each do |msg| %>
      <p class="text-danger"><%= msg %></p>
    <% end %>

    <%= form_for(@new_device, :url => system_devices_path, :html => {:class => 'form-inline'}) do |f| %>
      <div class="form-group">
        <%= f.text_field :name, :placeholder => 'Enter the name of the system', :class => 'form-control' %>
      </div>
      <button type="submit" class="btn btn-default">Create System Token</button>
    <% end %>
  </p>
</div>
